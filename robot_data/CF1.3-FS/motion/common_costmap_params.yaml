footprint: '[[1.8, 0.76], [-0.4, 0.76], [-0.4, 0.3], [-1.63, 0.3], [-1.63, -0.3], [-0.4, -0.3], [-0.4, -0.76], [1.8, -0.76]]'
footprint_padding: 0.000

inflation_radius: 0.8

transform_tolerance: 0.9
map_type: costmap

always_send_full_costmap: true

obstacle_layer:
  enabled: true
  obstacle_range: 14.0
  raytrace_range: 15.0
  inflation_radius: 0.2
  track_unknown_space: true
  combination_method: 1
  max_obstacle_height: 3.0
  observation_sources: obstacle_points
  obstacle_points: {data_type: PointCloud2, topic: /filtered_cloud, marking: true, clearing: true, inf_is_valid: true, min_obstacle_height: -3.1, max_obstacle_height: 3.0, observation_persistence: 0.5}

inflation_layer:
  enabled: false
  cost_scaling_factor: 10.0  # exponential rate at which the obstacle cost drops off (default: 10)
  inflation_radius: 0.5  # max. distance from an obstacle at which costs are incurred for planning paths.

static_layer:
  enabled: false
  map_topic: "/map"

FILTER_MINX: -1.6
FILTER_MINY: -0.7
FILTER_MINZ: -1.0
FILTER_MAXX: -0.5
FILTER_MAXY: 0.7
FILTER_MAXZ: 3.0
FILTER_OFFSET_X: 0.05
FILTER_OFFSET_Y: 0.05
FILTER_OFFSET_Z: 0.05
ENABLE_AUTO_FILTER: 1
ENABLE_FILTER_TYPE: 1
